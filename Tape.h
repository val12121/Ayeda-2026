#include <iostream>
#include <vector>

using namespace std; 

class Tape {
  private:
    bool color; // color == true ? black : white 
    int sizeX;
    int sizeY;

    vector<bool> tape_; 
    

  public:
    Tape() {
      
    }
};